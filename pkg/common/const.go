package common

// MetaKeyTTL is the metadata key under which a message TTL is stored.
const MetaKeyTTL = "ttl"

// MetaKeyBarrier is the metadata key under which Raft barrier timeout is
// stored.
const MetaKeyBarrier = "barrier"

const ClientIDSize = 16
